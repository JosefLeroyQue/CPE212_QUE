---
- name: Create multiple files
  hosts: all
  tasks:
    - name: Create multiple files
      ansible.builtin.file:
        path: "{{ ansible_facts['env']['HOME'] }}/{{ item }}"
        state: touch
      loop:
        - file1.txt
        - file2.txt
        - file3.txt
