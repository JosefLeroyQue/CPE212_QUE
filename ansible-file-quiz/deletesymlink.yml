---
- name: Delete a symbolic link
  hosts: all
  tasks:
    - name: Remove symbolic link
      ansible.builtin.file:
        path: "{{ ansible_facts['env']['HOME'] }}/source_link.txt"
        state: absent
