---
- name: Create a directory
  hosts: all
  tasks:
    - name: Create ansible-directory
      ansible.builtin.file:
        path: "{{ ansible_facts['env']['HOME'] }}/ansible-directory"
        state: directory
