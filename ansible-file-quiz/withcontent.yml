---
- name: Create a file with content
  hosts: all
  tasks:
    - name: Create student information file
      ansible.builtin.copy:
        dest: "{{ ansible_facts['env']['HOME'] }}/student_info.txt"
        content: |
          Name: Leroy
          Course: Computer Engineering
          Topic: Ansible File Module
