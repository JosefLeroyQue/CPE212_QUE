---
- name: Create an empty file
  hosts: all
  tasks:
    - name: Create ansible_test.txt
      ansible.builtin.file:
        path: "{{ ansible_facts['env']['HOME'] }}/ansible_test.txt"
        state: touch
